# Read Page bodies as storage format

The Note is markdown either way. Storage is the source because it still names the target Page and the Attachment filename, so Page links and Embeds resolve inside the Vault. Rendered HTML is easier to convert and was rejected because those links usually survive only as absolute Site URLs, which breaks offline reading.
