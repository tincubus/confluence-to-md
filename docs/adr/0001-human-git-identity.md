# Commits use the human git identity only

Agents default to adding a Cursor or generated-by trailer. This repo's history must not. Author and committer stay the repository git identity, and commit text describes the change rather than the tool that wrote it, because a public history is painful to rewrite once a trailer lands.
